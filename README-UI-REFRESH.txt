# Fine Dining Coach – UI Refresh (drop-in)

This zip contains updated front-end assets only.

Files:
- templates/index.html
- static/styles.css
- static/app.js

How to install:
1) Replace these files in your project using the same paths.
2) Ensure your Flask app exposes GET /_ping (returns 200) so the status button turns green.
3) Hard refresh your browser (Ctrl/Cmd-Shift-R).

Tip: you can deep-link and auto-run search with: /?zip=87124&radius_miles=3
